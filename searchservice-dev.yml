server:
  port: 8083
mydocument:
  service:
    storageServiceEndPoint: http://localhost:8081/storage
  aws:
    cloudsearch:
      searchEndPointUrl: ${MYDOCUMENT_CLOUDSEARCH_ENDPOINT}
      serviceEndpoint: ${MYDOCUMENT_CLOUDSEARCH_ENDPOINT}
      signingRegion: us-east-1
